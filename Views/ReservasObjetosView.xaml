<Window x:Class="ReservasApp.Views.ReservasObjetosView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Reservas - Objetos"
        Height="600"
        Width="1000"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#F4F6F8">

    <Grid Margin="30">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>


        <!-- ENCABEZADO -->

        <StackPanel Grid.Row="0"
                    Margin="0,0,0,25">

            <TextBlock Text="Reservas"
                       FontSize="28"
                       FontWeight="Bold"
                       Foreground="#1E293B"/>

            <TextBlock Text="Consulta y búsqueda de reservas registradas"
                       FontSize="14"
                       Foreground="#64748B"
                       Margin="0,5,0,0"/>

        </StackPanel>


        <!-- FILTRO DE FECHA -->

        <Border Grid.Row="1"
                Background="White"
                CornerRadius="10"
                Padding="18"
                Margin="0,0,0,15"
                BorderBrush="#E2E8F0"
                BorderThickness="1">

            <Grid>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="220"/>
                    <ColumnDefinition Width="110"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>


                <!-- TEXTO -->

                <TextBlock Grid.Column="0"
                           Text="Fecha:"
                           VerticalAlignment="Center"
                           Foreground="#334155"
                           FontSize="14"
                           FontWeight="SemiBold"
                           Margin="0,0,12,0"/>


                <!-- DATEPICKER -->

                <DatePicker Grid.Column="1"
                            Height="40"
                            SelectedDate="{Binding FechaBusqueda}"
                            FontSize="14"
                            VerticalContentAlignment="Center"
                            Margin="0,0,12,0"/>


                <!-- BOTÓN BUSCAR -->

                <Button Grid.Column="2"
                        Content="🔍  Buscar"
                        Height="40"
                        FontSize="14"
                        FontWeight="SemiBold"
                        Foreground="White"
                        Background="#1E3A5F"
                        BorderThickness="0"
                        Cursor="Hand"
                        Command="{Binding BuscarCommand}">

                    <Button.Template>

                        <ControlTemplate TargetType="Button">

                            <Border Background="{TemplateBinding Background}"
                                    CornerRadius="7">

                                <ContentPresenter
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"/>

                            </Border>

                        </ControlTemplate>

                    </Button.Template>

                </Button>

            </Grid>

        </Border>


        <!-- TABLA -->

        <Border Grid.Row="2"
                Background="White"
                CornerRadius="10"
                Padding="10"
                BorderBrush="#E2E8F0"
                BorderThickness="1">

            <DataGrid ItemsSource="{Binding Reservas}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      Background="White"
                      Foreground="#334155"
                      BorderThickness="0"
                      GridLinesVisibility="Horizontal"
                      HorizontalGridLinesBrush="#E2E8F0"
                      RowBackground="White"
                      AlternatingRowBackground="#F8FAFC"
                      HeadersVisibility="Column"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      CanUserResizeRows="False"
                      SelectionMode="Single"
                      SelectionUnit="FullRow"
                      FontSize="13"
                      RowHeight="38">

                <!-- ENCABEZADOS -->

                <DataGrid.ColumnHeaderStyle>

                    <Style TargetType="DataGridColumnHeader">

                        <Setter Property="Background"
                                Value="#1E3A5F"/>

                        <Setter Property="Foreground"
                                Value="White"/>

                        <Setter Property="FontWeight"
                                Value="SemiBold"/>

                        <Setter Property="FontSize"
                                Value="13"/>

                        <Setter Property="Padding"
                                Value="10"/>

                        <Setter Property="HorizontalContentAlignment"
                                Value="Left"/>

                        <Setter Property="BorderThickness"
                                Value="0"/>

                    </Style>

                </DataGrid.ColumnHeaderStyle>


                <!-- FILAS -->

                <DataGrid.RowStyle>

                    <Style TargetType="DataGridRow">

                        <Setter Property="Foreground"
                                Value="#334155"/>

                        <Style.Triggers>

                            <Trigger Property="IsMouseOver"
                                     Value="True">

                                <Setter Property="Background"
                                        Value="#EAF2F8"/>

                            </Trigger>

                            <Trigger Property="IsSelected"
                                     Value="True">

                                <Setter Property="Background"
                                        Value="#D9E8F5"/>

                                <Setter Property="Foreground"
                                        Value="#1E3A5F"/>

                            </Trigger>

                        </Style.Triggers>

                    </Style>

                </DataGrid.RowStyle>


                <!-- COLUMNAS -->

                <DataGrid.Columns>

                    <DataGridTextColumn Header="ID"
                                        Binding="{Binding ReservaId}"
                                        Width="70"/>

                    <DataGridTextColumn Header="Aula"
                                        Binding="{Binding NombreAula}"
                                        Width="*"/>

                    <DataGridTextColumn Header="Usuario"
                                        Binding="{Binding NombreUsuario}"
                                        Width="*"/>

                    <DataGridTextColumn Header="Fecha"
                                        Binding="{Binding Fecha, StringFormat={}{0:dd/MM/yyyy}}"
                                        Width="120"/>

                    <DataGridTextColumn Header="Hora"
                                        Binding="{Binding Hora}"
                                        Width="100"/>

                    <DataGridTextColumn Header="Motivo"
                                        Binding="{Binding Motivo}"
                                        Width="2*"/>

                </DataGrid.Columns>

            </DataGrid>

        </Border>

    </Grid>

</Window>
