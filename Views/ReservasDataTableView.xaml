<Window x:Class="ReservasApp.Views.ReservasDataTableView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Reservas - DataTable"
        Height="550"
        Width="950"
        WindowStartupLocation="CenterScreen"
        Background="#F4F6F8">

    <Grid Margin="30">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>


        <!-- ENCABEZADO -->

        <StackPanel Grid.Row="0"
                    Margin="0,0,0,25">

            <TextBlock Text="Reservas"
                       FontSize="28"
                       FontWeight="Bold"
                       Foreground="#1E293B"/>

            <TextBlock Text="Listado de reservas registradas"
                       FontSize="14"
                       Foreground="#64748B"
                       Margin="0,6,0,0"/>

        </StackPanel>


        <!-- TABLA -->

        <Border Grid.Row="1"
                Background="White"
                CornerRadius="10"
                Padding="10"
                BorderBrush="#E2E8F0"
                BorderThickness="1">

            <DataGrid ItemsSource="{Binding Reservas}"
                      AutoGenerateColumns="True"
                      AutoGeneratingColumn="DataGrid_AutoGeneratingColumn"
                      IsReadOnly="True"
                      Background="White"
                      Foreground="#334155"
                      BorderThickness="0"
                      GridLinesVisibility="Horizontal"
                      HorizontalGridLinesBrush="#E2E8F0"
                      VerticalGridLinesBrush="#E2E8F0"
                      RowBackground="White"
                      AlternatingRowBackground="#F8FAFC"
                      HeadersVisibility="Column"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      CanUserResizeRows="False"
                      SelectionMode="Single"
                      SelectionUnit="FullRow"
                      FontSize="13"
                      RowHeight="40"
                      ColumnHeaderHeight="42">

                <!-- ENCABEZADOS -->

                <DataGrid.ColumnHeaderStyle>

                    <Style TargetType="DataGridColumnHeader">

                        <Setter Property="Background"
                                Value="#1E3A5F"/>

                        <Setter Property="Foreground"
                                Value="White"/>

                        <Setter Property="FontWeight"
                                Value="SemiBold"/>

                        <Setter Property="FontSize"
                                Value="13"/>

                        <Setter Property="Padding"
                                Value="12,0"/>

                        <Setter Property="HorizontalContentAlignment"
                                Value="Left"/>

                        <Setter Property="VerticalContentAlignment"
                                Value="Center"/>

                        <Setter Property="BorderThickness"
                                Value="0"/>

                    </Style>

                </DataGrid.ColumnHeaderStyle>


                <!-- FILAS -->

                <DataGrid.RowStyle>

                    <Style TargetType="DataGridRow">

                        <Setter Property="Foreground"
                                Value="#334155"/>

                        <Style.Triggers>

                            <Trigger Property="IsMouseOver"
                                     Value="True">

                                <Setter Property="Background"
                                        Value="#EAF2F8"/>

                            </Trigger>

                            <Trigger Property="IsSelected"
                                     Value="True">

                                <Setter Property="Background"
                                        Value="#D9E8F5"/>

                                <Setter Property="Foreground"
                                        Value="#1E3A5F"/>

                            </Trigger>

                        </Style.Triggers>

                    </Style>

                </DataGrid.RowStyle>


                <!-- CELDAS -->

                <DataGrid.CellStyle>

                    <Style TargetType="DataGridCell">

                        <Setter Property="Padding"
                                Value="12,0"/>

                        <Setter Property="VerticalContentAlignment"
                                Value="Center"/>

                        <Setter Property="BorderThickness"
                                Value="0"/>

                    </Style>

                </DataGrid.CellStyle>

            </DataGrid>

        </Border>

    </Grid>

</Window>
    
    
    
    
    
    
    
    
    