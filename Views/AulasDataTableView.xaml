<Window x:Class="ReservasApp.Views.AulasDataTableView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Aulas - DataTable"
        Height="520"
        Width="800"
        WindowStartupLocation="CenterScreen"
        Background="#F4F6F8">

    <Grid Margin="30">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>


        <!-- ENCABEZADO -->

        <StackPanel Grid.Row="0"
                    Margin="0,0,0,25">

            <TextBlock Text="Aulas"
                       FontSize="28"
                       FontWeight="Bold"
                       Foreground="#1E293B"/>

            <TextBlock Text="Consulta de aulas registradas en el sistema"
                       FontSize="14"
                       Foreground="#64748B"
                       Margin="0,5,0,0"/>

        </StackPanel>


        <!-- SECCIÓN -->

        <Border Grid.Row="1"
                Background="#1E3A5F"
                CornerRadius="8"
                Padding="15"
                Margin="0,0,0,12">

            <TextBlock Text="📚  Lista de aulas"
                       Foreground="White"
                       FontSize="15"
                       FontWeight="SemiBold"/>

        </Border>


        <!-- TABLA -->

        <Border Grid.Row="2"
                Background="White"
                CornerRadius="10"
                Padding="10"
                BorderBrush="#E2E8F0"
                BorderThickness="1">

            <DataGrid ItemsSource="{Binding Aulas}"
                      AutoGenerateColumns="True"
                      IsReadOnly="True"
                      Background="White"
                      Foreground="#334155"
                      BorderThickness="0"
                      GridLinesVisibility="Horizontal"
                      HorizontalGridLinesBrush="#E2E8F0"
                      RowBackground="White"
                      AlternatingRowBackground="#F8FAFC"
                      HeadersVisibility="Column"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      CanUserResizeRows="False"
                      SelectionMode="Single"
                      SelectionUnit="FullRow"
                      FontSize="13"
                      RowHeight="38">

                <DataGrid.ColumnHeaderStyle>

                    <Style TargetType="DataGridColumnHeader">

                        <Setter Property="Background"
                                Value="#1E3A5F"/>

                        <Setter Property="Foreground"
                                Value="White"/>

                        <Setter Property="FontWeight"
                                Value="SemiBold"/>

                        <Setter Property="FontSize"
                                Value="13"/>

                        <Setter Property="Padding"
                                Value="10"/>

                        <Setter Property="HorizontalContentAlignment"
                                Value="Left"/>

                        <Setter Property="BorderThickness"
                                Value="0"/>

                    </Style>

                </DataGrid.ColumnHeaderStyle>


                <DataGrid.RowStyle>

                    <Style TargetType="DataGridRow">

                        <Setter Property="Margin"
                                Value="0,1"/>

                        <Setter Property="Foreground"
                                Value="#334155"/>

                        <Style.Triggers>

                            <Trigger Property="IsMouseOver"
                                     Value="True">

                                <Setter Property="Background"
                                        Value="#EAF2F8"/>

                            </Trigger>

                            <Trigger Property="IsSelected"
                                     Value="True">

                                <Setter Property="Background"
                                        Value="#D9E8F5"/>

                                <Setter Property="Foreground"
                                        Value="#1E3A5F"/>

                            </Trigger>

                        </Style.Triggers>

                    </Style>

                </DataGrid.RowStyle>

            </DataGrid>

        </Border>

    </Grid>

</Window>

